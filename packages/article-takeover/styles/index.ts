import { StyleSheet } from "react-native";

export default () =>
  StyleSheet.create({
    container: {
      flex: 1,
      borderWidth: 1,
      borderColor: "red",
      height: "100%",
      minHeight: 700,
    },
  });
